name: lease-expiry
inputs: { task: task-1, objective: expire safely }
steps:
  - expect: agent
    submit: { summary: planned, evidence: [{ key: summary, value: planned }] }
  - expect: agent
    submit: { summary: attempted, evidence: [{ key: summary, value: attempted }, { key: evidence, value: attempt }] }
  - expect: deterministic
    execute: { exit_code: 1 }
  - expect: agent
    submit: { summary: blocked, verdict: blocked, approval: false, evidence: [{ key: summary, value: blocked }, { key: verdict, value: blocked }, { key: approval, value: false }, { key: evidence, value: lease-expired }] }
expect_terminal: blocked
